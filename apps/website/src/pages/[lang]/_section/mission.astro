---
import HeaderSection from "@/components/header/section.astro";
import { getLangFromPath } from "@/i18n";
import i18n from "@istrust/i18n";

const lang = getLangFromPath(Astro.url.pathname);
---

<div class="mx-auto max-w-4xl p-6">
  <HeaderSection header={i18n("Mission", lang)}
    >{i18n("What set us apart", lang)}
  </HeaderSection>

  <div class="mt-6 flex flex-wrap justify-center gap-6 p-6">
    <div class="ring-border flex w-60 rounded-lg ring">
      <div class="flex flex-col p-4">
        <h2 class="font-semibold">{i18n("Free Forever", lang)}</h2>

        <p class="text-muted">
          {
            i18n(
              "No fees, no subscriptions. We believe online safety should be a right, not a luxury.",
              lang,
            )
          }
        </p>
      </div>
    </div>
    <div class="ring-border flex w-60 rounded-lg ring">
      <div class="flex flex-col p-4">
        <h2 class="font-semibold">{i18n("Fully Transparent", lang)}</h2>

        <p class="text-muted">
          {
            i18n(
              "Our code is 100% open source. Audit us or contribute on",
              lang,
            )
          }
          <a
            href="https://github.com/Internet-Society-Belgium/isTrust/"
            target="_blank"
            class="underline">Github</a
          ><span>.</span>
        </p>
      </div>
    </div>
    <div class="ring-border flex w-60 rounded-lg ring">
      <div class="flex flex-col p-4">
        <h2 class="font-semibold">{i18n("Private by Design", lang)}</h2>

        <p class="text-muted">
          {
            i18n(
              "Your data stays with you. We track nothing and send zero data to our servers.",
              lang,
            )
          }
        </p>
      </div>
    </div>
  </div>
</div>
